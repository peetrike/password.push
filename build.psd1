@{
    ModuleManifest           = 'src/password.push.psd1'
    # Subsequent relative paths are relative to the ModuleManifest
    OutputDirectory          = '../Release'
    VersionedOutputDirectory = $false
    Suffix                   = '_init.ps1'
}
